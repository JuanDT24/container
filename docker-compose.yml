version: "3.8"

services:
  benchmark:
    image: alpine:latest
    container_name: benchmark-runner
    volumes:
      - .:/host  
    working_dir: /host  
    entrypoint: ["/bin/sh", "-c"]
    command: ["chmod +x benchmark.sh && ./benchmark.sh"]
